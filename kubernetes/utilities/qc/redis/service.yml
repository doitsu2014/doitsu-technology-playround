apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis
spec:
  selector:
    app: redis-master
  ports:
  - name: http
    port: 6379
    targetPort: 6379
    nodePort: 32204
  type: NodePort